#include <iostream>

int main() {
    double a = 1.0 / 3;
    double b = 2.0 / 6;

    std:: cout << a << " " << b << std::endl;

    bool expression = (a > b);

    std::cout << expression << std::endl;
    
    return 0;
}