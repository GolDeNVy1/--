#include <iostream>

int main() {
    int number;
    std::cout << "Enter a number: ";

    while (number > 0) {
        std::cin >> number;
        }

    int number;

    do {
        std::cin >> number;
    } while (number > 0);
    }

